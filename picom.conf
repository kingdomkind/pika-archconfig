backend = "glx";

# ANIMATION CONFIGURATION FOR PIJULIUS
animations = true;
animation-stiffness = 350;
animation-dampening = 35;
animation-for-open-window = "zoom";
animation-for-menu-window = "zoom";
animation-for-transient-window = "zoom";
animation-clamping = true;
animation-mass = 1;

# ANIMATION CONFIGURATION FOR YXSHUI
#animations = (
#	{
#		triggers = [ "open" ];
#		preset = "appear";
#		scale = 0;
#		duration = 0.25;
#	},
#	{
#		triggers = [ "close" ];
#		preset = "disappear";
#		scale = 0.5;
#		duration = 0.125;
#	},
#	{
#		triggers = [ "geometry" ];
#		preset = "geometry-change";
#	},
#)

fading = true;
fade-in-step = 0.03;
fade-out-step = 0.1;
fade-delta = 15;
no-fading-openclose = false;

blur-method = "dual_kawase";
blur-size = 1;
blur-strength = 6;
vsync = true;
#corner-radius = 20

#rules = (
#	# Rule to ONLY round floating window
#	{
#  		match = "FLOATING@ = 1";
#    		corner-radius = 20;
#  	},
#);

#rules = (
#	{
#		match = "FLOATING@ != 1";
#		animations = ( { triggers = [ "geometry" ]; preset = "geometry-change"; }, );
#	}
#);


